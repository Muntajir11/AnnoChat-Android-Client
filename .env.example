# Android uses src/config/config.ts (__DEV__ emulator host 10.0.2.2).
# No secrets live in the app. Tickets are minted by the Node server.
# Optional TURN for video NAT (production values, not used in emulator STUN-only).
# TURN_URL=
# TURN_USER=
# TURN_PASS=
